Lang.lam: Parsec(Core.Ctx -> Core)
  def C = Core.Ctx -> Core
  Parsec.bind<Pair(Bool)(String)><C>
  | Lang.enclose<String>(Lang.nam(Bool.true));
  | (x) x<() Parsec(C)> | (eras) (n)
  Parsec.then<Unit><C>(Lang.space) |
  Parsec.bind<C   ><C>(Lang.trm)   | (body)
  Parsec.pure<C>
  | (ctx)
    let tbody = ((x) body(Core.Ctx.ext(n)(x)(ctx))) :: Core -> Core
    Core.lam(eras)(n)(tbody);;;;;

