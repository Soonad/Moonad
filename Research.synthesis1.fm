// The synthesizer can generate functions definition like composition
Research.synthesis1
  : <P: Nat -> Type> ->
    (f: <n: Nat> -> P(n) -> P(Nat.succ(n))) ->
    (x: P(Nat.zero)) ->
    P(Nat.3)
  _
