//Finds the first occurrence that satisfies a condition
List.find: <A: Type> -> (A -> Bool) -> List(A) -> Maybe(A)
  <A> (cond) (xs)
  xs<() Maybe(A)>
  | Maybe.none<A>;
  | (head) (tail)
    cond(head)<() Maybe(A)>
    | Maybe.some<A>(head);
    | List.find<A>(cond)(tail);;

