// Throw a custom error message

Parsec.fail: <A: Type> -> String -> Parsec(A)
  <A> (msg)
  <X> (s) () (empty_error) () ()
  s<() X> | () (index) ()
    let e = List.single<String>(msg)
    empty_error(s)(Parsec.Error.make_fancy(index)(e));
