Parsec.State.make:
  <S: Type> ->
  <E: Type> ->
  String ->
  Nat ->
  List(Parsec.Error(E)) ->
  S ->
  Parsec.State(S)(E)

  <S> <E> (input) (index) (error) (state)
  <P> (make) make(input)(index)(error)(state)
