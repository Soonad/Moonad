// take a list of parsers and try them until one consumes input or succeeds
Parsec.choice: <A: Type> -> List(Parsec(A)) -> Parsec(A)
  <A> (ps)
  ps<() Parsec(A)>
  | Parsec.zero<A>;
  | (head) (tail) Parsec.plus<A>(head)(Parsec.choice<A>(tail));
