Jacobian.precompute_window: Jacobian -> (w: Nat) -> List(Jacobian)
  (p) (w) let windows = ecdsa.window(w); let points = Concat.empty<Jacobian>; let size = Nat.pow(Nat.2)(Nat.pred(w)); Concat.to_list<Jacobian>(Jacobian.precompute_window.tco(p)(windows)(points)(Nat.0)(Jacobian.0)(size)(Nat.0))

