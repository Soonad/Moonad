Jacobian.to_pointBatch: (ps: List(Jacobian)) -> List(Point)
  (ps) let inverts = ecdsa.invertBatch(List.map<Jacobian><Nat>((p) p<(self) Nat>((p.x) (p.y) (p.z) p.z))(ps))(CURVE.P); Jacobian.to_pointBatch.aux(ps)(inverts)

