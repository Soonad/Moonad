TA.Thing.blink: (self: TA.Thing) -> (at_tik: F64) -> (to_pos: F64.V3) -> TA.Thing
  (self) (at_tik) (to_pos) let rooted = TA.Thing.is_rooted(self); let self.tik = TA.Thing.get_tik(self); let self.pos = TA.Thing.get_pos(self); rooted<() TA.Thing>(self)(let new_pos = Bool.if<F64.V3>(F64.eql(self.tik)(at_tik))(to_pos)(self.pos); TA.Thing.set_pos(self)(new_pos))

