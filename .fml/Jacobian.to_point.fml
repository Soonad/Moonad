Jacobian.to_point: Jacobian -> Point
  (p) p<(x1) Point>((p.x) (p.y) (p.z) let invz = ecdsa.invertUnsafe(p.z)(CURVE.P); let invz2 = Nat.square(invz); let x = ecdsa.mod(Nat.mul(p.x)(invz2)); let y = ecdsa.mod(Nat.mulVar(Nat.3)(p.y)(invz2)(invz)); Point.new(x)(y))

