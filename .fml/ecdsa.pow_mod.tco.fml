ecdsa.pow_mod.tco: Nat -> Nat -> Nat -> Nat -> Nat
  (a) (b) (modulo) (res) Bool.if<Nat>(Nat.eql(b)(0))(res)(let res = Bool.if<Nat>(Nat.odd(b))(Nat.mod(Nat.mul(res)(a))(modulo))(res); let b = Nat.div(b)(2); let a = Nat.mod(Nat.mul(a)(a))(modulo); ecdsa.pow_mod.tco(a)(b)(modulo)(res))

