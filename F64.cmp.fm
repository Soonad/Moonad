// TODO
F64.cmp: F64 -> F64 -> Cmp 
  (a) (b)
  a<() Cmp> | (a.word)
  b<() Cmp> | (b.word)
  F64.eql(a)(b)<() Cmp>
  | Cmp.eql;
  | Word.ltn<Nat.64>(a.word)(b.word)<() Cmp>
    | Cmp.ltn;
    | Cmp.gtn;;;;
