V3.norm: V3 -> V3 
  (v)
  v<() V3> | (v.x) (v.y) (v.z)
  let len = V3.len(v)
  let new_x = F64.div(v.x)(len)
  let new_y = F64.div(v.y)(len)
  let new_z = F64.div(v.z)(len)
  V3.new(new_x)(new_y)(new_z);
