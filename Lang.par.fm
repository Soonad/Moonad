Lang.par: Parsec(Core.Ctx -> Core)
  def C = Core.Ctx -> Core
  Parsec.then<String><C>(Lang.symbol("(")) |
  Parsec.bind<C     ><C>(Lang.trm)         |  (term)
  Parsec.then<Unit  ><C>(Lang.space)       |
  Parsec.then<String><C>(Parsec.tokens(")"))      |
  Parsec.pure<C>(term);;;;
