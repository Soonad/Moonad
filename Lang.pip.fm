Lang.pip: (Core.Ctx -> Core) -> Parsec(Core.Ctx -> Core)
  def C = Core.Ctx -> Core
  (func)
  Parsec.then<Unit><C>(Lang.space)               |
  Parsec.then<String><C>(Lang.symbol("|"))       |
  Parsec.bind<C><C>(Lang.trm)                    | (argm)
  Parsec.then<Unit><C>(Lang.space)               |
  Parsec.then<String><C>(Parsec.tokens(";"))            |
  Parsec.pure<C>
  | (ctx) Core.app(Bool.false)(func(ctx))(argm(ctx));;;;;;
