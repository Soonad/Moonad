Parsec.many: <A: Type> -> Parsec(A) -> Parsec(List(A))
  <A> (p)
  Parsec.bind<Maybe(A)><List(A)>
  | Parsec.optional<A>(p);
  | (x) x<() Parsec(List(A))>
    | Parsec.pure<List(A)>(List.nil<A>);
    | (a) Parsec.map<List(A)><List(A)>
      | List.cons<A>(a);
      | Parsec.many<A>(p);;;


