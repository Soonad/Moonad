Lang.trm: Parsec(Core.Ctx -> Core)
  def C = Core.Ctx -> Core
  Parsec.bind<C><C>
  | Parsec.choice<C> |
    List.cons<Parsec(C)>(Lang.let)                 |
    List.cons<Parsec(C)>(Lang.typ)                 |
    List.cons<Parsec(C)>(Lang.var)                 |
    List.cons<Parsec(C)>(Parsec.try<C>(Lang.all))  |
    List.cons<Parsec(C)>(Parsec.try<C>(Lang.lam))  |
    List.cons<Parsec(C)>(Lang.par)                 |
    List.nil< Parsec(C)>;;;;;;;;
  | (x) Parsec.pure<C>(x); //Lang.pst(x);
