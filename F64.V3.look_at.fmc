V3.look_at: V3 -> V3 -> V3 -> V3 
  (a) (b) (c)
  a<() V3> | (a.x) (a.y) (a.z)
  b<() V3> | (a.x) (a.y) (a.z)
  c<() V3> | (a.x) (a.y) (a.z)
  let a_eql_b  = V3.eql(a)(b)
  let diff     = V3.sub(b)(a)
  let normdiff = V3.norm(diff)
  a_eql_b<() V3> 
  | normdiff ;
  | c ;;;;
   
