ecdsa.invertBatch.aux2: Π(nums:(List Nat)) Π(modulo:Nat) Π(acc:Nat) Π(scratch:(List Nat)) Π(inverts:(List Nat)) (List Nat)
  λnums λmodulo λacc λscratch λinverts ((<nums λnums.self (List Nat)> inverts) λnums.head λnums.tail ((<scratch λscratch.self (List Nat)> inverts) λscratch.head λscratch.tail ((<((Nat.eql nums.head) 0) λself.self (List Nat)> $inverts=((<List.cons Nat> 0) inverts);(((((ecdsa.invertBatch.aux2 nums.tail) modulo) acc) scratch.tail) inverts)) $inverts=((<List.cons Nat> ((Nat.mod ((Nat.mul acc) scratch.head)) modulo)) inverts);$acc=((Nat.mod ((Nat.mul acc) nums.head)) modulo);(((((ecdsa.invertBatch.aux2 nums.tail) modulo) acc) scratch.tail) inverts))))

