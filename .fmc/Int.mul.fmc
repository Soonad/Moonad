Int.mul: Π(a:Int) Π(b:Int) Int
  λa λb (<a λa. Int> λa.x λa.y (<b λb. Int> λb.x λb.y ((Int.new ((Nat.add ((Nat.mul a.x) b.x)) ((Nat.mul a.y) b.y))) ((Nat.add ((Nat.mul a.x) b.y)) ((Nat.mul a.y) b.x)))))

