Parsec.char : Char -> Parsec(Char)
  (c)
  Parsec.token<Char>
  | (x) U16.eql(c)(x)<() Maybe(Char)>
    | Maybe.some<Char>(c);
    | Maybe.none<Char>;;
  | List.single<Parsec.Error.Item>
    | Parsec.Error.Item.tokens(String.single(c));;
