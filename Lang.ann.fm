Lang.ann: (Core.Ctx -> Core) -> Parsec(Core.Ctx -> Core)
  def C = Core.Ctx -> Core
  (expr)
  Parsec.then<Unit  ><C>(Lang.space)        |
  Parsec.then<String><C>(Lang.symbol("::")) |
  Parsec.bind<C     ><C>(Lang.trm)          |  (type)
  Parsec.pure<C     >
  | (ctx) Core.ann(Bool.false)(expr(ctx))(type(ctx));;;;
