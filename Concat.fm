
//// A concatenation list.
//Concat: (A: Type) -> Type
  //(A) List(A) -> List(A)

//Concat.push: <A: Type> -> A -> Concat(A) -> Concat(A)
  //<A> (val) (con) (x) con(List.cons<>(val)(x))

//// Converts a list to a concatenation list.
//Concat.to_list: <A: Type> -> (xs: Concat(A)) -> List(A)
  //<A> (xs) xs(List.nil<A>)

