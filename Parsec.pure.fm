// We can turn any value into a parser that returns that value
// without consuming any input.

Parsec.pure: <A: Type> -> (a: A) -> Parsec(A)
  <A> (a)
  <X> (s) (empty_value) () () () empty_value(s)(a)
